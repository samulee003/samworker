# SamWorker — UI REQUIREMENTS(設計契約)
## 用途:本檔是 design.md(Antigravity + Stitch MCP 生成)的輸入契約。
## 規則:design.md 的視覺方案必須覆蓋本檔列出的每一個數據欄位、狀態與動作。佈局、配色、字體、圓角、圖標風格由設計自由發揮;「顯示什麼、何時顯示、按了會怎樣」不可增刪。

---

## 0. 產品性格

- 定位:治理型工作助手——「所有 AI 動你電腦前,先過你這一關」
- 氣質:冷靜、資訊密度高、可信賴。不是玩具,是**值班台**
- 明暗雙主題必須支援;中文(繁)為主語言,文案欄位按本檔提供的為準
- 視覺階層必須讓「需要你決定的事」最搶眼,「它正在幹活」次之,「幹完了」最弱

---

## 1. 畫面清單(共 6 個)

### S1 任務對話屏(主屏)
- 佈局:左=任務對話流(主),右=即時狀態側欄(輔)。側欄可折疊
- 對話流每條任務必須能呈現這些**任務階段徽章**(狀態機一一對應):
  `計劃中 → 執行中 → 驗證中 → 交付中 → 完成`,旁路狀態:`反思中 / 待澄清 / 失敗`
- 每個執行步驟的卡片必須顯示:工具名、目標摘要(如 `fs.writeFile → work/out.md`)、耗時、結果(成功/被攔/錯誤)
- 側欄(即時狀態)必須顯示:
  - 當前狀態機階段(醒目)
  - 進度 `步驟 3/7`
  - 熔斷器狀態三態燈:`正常(綠)/ 計數中(黃,n/3)/ 已熔斷(紅,冷卻 mm:ss)`——冷卻必須是**活的倒數**
  - 本任務審計計數:允許 n / 攔截 n / 錯誤 n(點擊跳轉 S4)
- 任務完成時,對話流出現**交付卡片**:交付物文件名(可點開預覽)、動過的檔案數、風險提示數、`查看完整報告` 按鈕(開 REPORT.md)

### S2 審批卡片(全產品核心,出現在對話流內,阻塞任務)
觸發:Guardian 判定需要用戶批准的工具調用。必須顯示:
- **操作摘要**(一句人話):如「移動 217 個檔案到 work/sorted/」
- **工具與目標**:`fs.writeFile → work/report.md`
- **觸發規則**:規則 id + 人話解釋(如 `fs-write-overwrite:覆寫既有檔案`)
- **風險等級**(三級,視覺必須可瞬間分辨):常規 / 覆寫既有 / 真實世界動作
- **授權範圍選項**(每次審批必須可選,這是產品靈魂):
  1. `僅這一次`
  2. `這類操作以後都允許`(allow_same_scope,附說明:同規則同範圍)
  3. `拒絕`
  - 註:bridge 寫操作/桌面操作**永遠不出現**「以後都允許」選項(安全鐵律 8)——卡片在這些場景只顯示 `僅這一次 / 拒絕`,並顯示原因小字:「真實世界動作永遠逐次確認」
- 按鈕語義回調:`approve_once` / `approve_scope` / `deny`(不可用 Enter 默認批准;拒絕永遠可用鍵盤直達)

### S3 異常卡片(4 種,替代一切裸紅字錯誤)
| 卡片 | 觸發 | 必須顯示 | 動作 |
|------|------|---------|------|
| 安全攔截(黃) | Guardian deny | 操作、目標、觸發規則、人話原因 | `[知道了]` `[查看規則]` |
| 執行失敗(紅) | 工具拋錯 | 步驟、錯誤摘要、`AI 正在重試(第 n/2 次)` | 無(自動);2 次後轉灰卡 |
| 熔斷觸發(深紅) | 熔斷器 OPEN | 連續被拒原因摘要、冷卻倒數 mm:ss(活倒數) | `[手動重置]` |
| 任務失敗(灰,終態) | 反思 2 次仍敗 | 已完成步驟數 x/y、失敗原因、**安心文案:「已完成的操作都記錄在審計日誌」** | `[查看審計日誌]` `[重新嘗試]` |

### S4 審計查看器
- 數據源:`.samworker/audit.log`(JSONL)。必須提供:
  - 表格:時間 / 調用 ID / 工具 / 目標 / 規則 / 結果(allow·deny·error 三色)
  - 篩選:按任務、按工具、按結果;關鍵字搜索
  - 單條展開:完整參數(截斷至 500 字符,與審計落盤一致)
- 鐵律顯示:`vision.verify` 行**不顯示圖片**,只顯示 `已執行 + 結論`(安全鐵律 10)

### S5 定時任務管理
- 任務列表:名稱 / cron 人話(「每分鐘」「每天 09:00」)/ 上次結果(成功/失敗/錯過)/ 上次運行時間 / 啟用開關
- 動作:`[新建]`(彈窗:名稱+cron+任務描述)/ `[刪除]`(需確認,走 S2 審批)/ `[查看記錄]`(跳 S6)
- 空狀態文案:「還沒有定時任務。讓 SamWorker 在你睡覺時幹活。」

### S6 運行記錄回放(transcript)
- 選定一個 job 的某次運行:時間線逐條回放該次運行的每個事件(step / audit / result)
- 每條:時間戳、事件類型徽章、內容摘要;審計事件可展開(同 S4 單條)
- 頂部:該次運行的結論(成功/失敗)+ 耗時;若是 catchup 啟動(錯過後補跑),必須顯示 `補跑` 徽章

---

## 2. 全局元素

- 頂欄:產品名 SamWorker、當前任務狀態膠囊、熔斷器狀態燈(S1 規則)、設置(模型 key、規則集路徑、審計路徑)
- 設置頁最小集:模型供應商/key、`permissions.json` 路徑與即時編輯、`.samworker/` 數據目錄位置、`[導出審計]`(下載 JSONL)
- 所有確認對話框禁止「Enter 默認同意」;破壞性動作按鈕與常規按鈕必須視覺隔離

---

## 3. 交接驗收(design.md 生成後人工核對)

- [ ] S2 審批卡片:5 個信息區+授權選項含「無 scope 選項」變體
- [ ] S3 四卡:黃/紅/深紅/灰,熔斷含倒數設計
- [ ] S1 側欄:狀態機徽章 + 熔斷三態燈 + 審計計數
- [ ] S4 審計表:三色結果 + vision 行無圖設計
- [ ] S5/S6:job 人話 cron、catchup 徽章、回放時間線
- [ ] 明暗雙主題各出一版
- [ ] 每個界面標註了數據來源欄位名(與本檔對照)

## 4. 給 Stitch 的提示詞開頭(建議)

> 為「SamWorker——治理型 AI 工作助手」設計網頁 UI。氣質:冷靜、高密度、可信賴的值班台,非玩具感。核心交互是「審批卡片」:AI 動用戶檔案前必須彈卡徵求批准,選項含「僅這一次/這類以後都允許/拒絕」。按附上的 UI-REQUIREMENTS 契約逐屏設計 6 個畫面+明暗雙主題。
